{
  "name": "zoomo-repair-queue",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.2.0",
    "@testing-library/user-event": "^13.5.0",
    "firebase": "^10.1.0", // Updated Firebase version
    "react": "^18.2.0", // Updated React version
    "react-dom": "^18.2.0", // Updated React DOM version
    "react-router-dom": "^6.3.0", // Updated React Router DOM version
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build", // Added for GitHub Pages
    "deploy": "gh-pages -d build" // Added for GitHub Pages
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://mahendra-zoomo.github.io/zoomo-repair-queue" // Added for GitHub Pages
}